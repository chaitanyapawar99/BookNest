package com.cdac.dto;

import lombok.Data;

@Data
public class CreateOrderRequest {
    // Add any fields needed for order creation
    // For now, it's empty as the current implementation only needs userId
}
